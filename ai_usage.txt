I used artificial intelligence, namely chat gpt to generate a script, namely for this line:` ps --sort=-%mem -eo pid,%mem,comm | head -n 11`.
I also used it to generate all the code for the system file.
And he helped me with the commands to run the script correctly and get the information correctly.
